# python / venv
.venv/
__pycache__/
*.pyc

# MLX LoRA / fused / GGUF outputs (regenerable from train.sh + fuse.sh + export_gguf.sh)
**/adapters/
**/fused_model/
**/gguf/
.llama.cpp/

# large / cached artifacts
*.safetensors
*.gguf
.DS_Store

# keep: dataset jsonl + benchmark result json (they're small and part of the demo)
